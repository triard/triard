name: Generate Snake Animation

on:
  # Menjalankan otomatis setiap 24 jam sekali
  schedule:
    - cron: "0 0 * * *" 
  
  # Mengizinkan Anda menjalankan action secara manual
  workflow_dispatch:
  
  # Dijalankan otomatis setiap kali ada perubahan di branch main
  push:
    branches:
    - main
    
jobs:
  generate:
    permissions: 
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
      # Generate animasi ular dari kontribusi triard
      - name: generate github-contribution-grid-snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: triard
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
          
      # Push gambar yang sudah di-generate ke branch 'output'
      - name: push github-contribution-grid-snake.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
